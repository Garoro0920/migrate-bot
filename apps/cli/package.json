{
  "name": "@migrate-bot/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "tsx src/index.ts",
    "migrate": "tsx src/index.ts migrate"
  },
  "dependencies": {
    "@migrate-bot/agent": "workspace:*",
    "@migrate-bot/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4",
    "typescript": "6.0.3",
    "vitest": "4.1.5"
  }
}
