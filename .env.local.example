# Anthropic API key for the Migration Agent.
# Required when running `analyze` without the --no-llm flag.
# Obtain from https://console.anthropic.com/settings/keys
# Do NOT commit the real .env.local — it is gitignored.
ANTHROPIC_API_KEY=sk-ant-...
